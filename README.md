# M4 – Nonlinear ARX Estimation of Building Indoor Temperature

This code implements a statisitcal forecasting model for the estimation of indoor air temperature in a school classroom. The model extends a first-order ARX structure with a nonlinear sigmoid function for the thermostatic valve and a B-spline decomposition for the time-varying solar gain, fitted to FMU simulation data with additive Gaussian noise.

## Abstract

An ARX model with nonlinear inputs is fitted to thermal data of a classroom zone generated by a Functional Mock-up Unit (FMU) exported from Modelica. The nonlinear parameters governing the sigmoid valve function and the solar B-spline profile are estimated using the Sequential Least Squares Programming (SLSQP) algorithm in Python. Linear parameters are recovered analytically by least squares. The estimated model is compared against both the FMU output and the experimental measurements.

## Data

| File | Description |
|------|-------------|
| `borgerskolen_experiment.csv` | Experimental time series: indoor temperature $Y_t$, ambient temperature $T_{a,t}$, setpoint $T_{\text{set},t}$, solar power $P_{s,t}$, forward temperature $T_{f,t}$ |
| `TemperaturePIcontroller.csv` | PI controller simulation time axis |
| `TemperatureC001.csv` | FMU-simulated indoor temperature |

Measurements are sampled every 30 minutes (1294 observations) from 2021-03-01 to 2021-03-27.

## ARX room temperature model

The M4 model for the indoor temperature prediction $Y_t$ at time $t$ is

$$Y_t = \phi_1 Y_{t-1} + \sum_{i=1}^{3} \beta_i x_{i,t-1} + \varepsilon_t \tag{1}$$

where $\phi_1$ is the autoregressive coefficient, $\beta_i$ are linear parameters multiplying the $i$-th input, and $\varepsilon_t \sim \mathcal{N}(0, \sigma^2)$ is an i.i.d. white noise process.

The three inputs in Eq. (1) are defined as follows.

**Ambient heat exchange**

$$x_{1,t} = T_{a,t} - Y_t \tag{2}$$

where $T_{a,t}$ is the ambient outside air temperature.

**Thermostatic valve — sigmoid function**

$$x_{2,t} = \frac{T_{f,t} - Y_t}{1 + \exp\!\bigl(-\alpha_1(T_{\text{set},t} + \alpha_2 - Y_t)\bigr)} \tag{3}$$

where $T_{f,t}$ is the forward temperature of the hot water entering the radiator, $T_{\text{set},t}$ is the temperature setpoint, and $\alpha_1$, $\alpha_2$ are the nonlinear parameters governing the slope and offset of the valve response.

**Solar gain — B-spline decomposition**

$$x_{3,t} = P_{s,t} \sum_{i=1}^{M} \eta_i B_{i,k}(t) \tag{4}$$

where $P_{s,t}$ is the measured solar power, $\eta_i$ are the scaling coefficients of the $i$-th B-spline basis function, and $B_{i,k}$ is the de Boor B-spline of order $k$ defined on a 24-hour knot grid.

## Parameter estimation

The nonlinear parameters $\boldsymbol{\theta}_{\text{nl}} = [\alpha_1,\, \alpha_2,\, \eta_1, \ldots, \eta_M]^\top$ are estimated by minimising the sum of squared errors

$$S(\boldsymbol{\theta}) = (\mathbf{Y} - \mathbf{X}\boldsymbol{\theta})^\top (\mathbf{Y} - \mathbf{X}\boldsymbol{\theta}) \tag{5}$$

using the SLSQP solver in SciPy with a tolerance of $\varepsilon = 10^{-5}$ and a maximum of $10^4$ iterations. For fixed nonlinear parameters, the linear parameters $\boldsymbol{\theta}_{\text{lin}} = [\phi_1,\, \beta_1,\, \beta_2,\, \beta_3]^\top$ are obtained analytically from the normal equations

$$\hat{\boldsymbol{\theta}}_{\text{lin}} = (\mathbf{X}^\top \mathbf{X})^{-1} \mathbf{X}^\top \mathbf{Y} \tag{6}$$

where $\mathbf{X}$ is the design matrix.
## Parameters

**Estimation settings**

| Parameter | Value | Description |
|-----------|-------|-------------|
| $\sigma^2$ | 0.1 | Variance of additive Gaussian noise $\varepsilon_t$ |
| $m$ | 1295 | Number of noise samples |
| $M$ | 4 | Number of B-spline basis functions |
| $k$ | 2 | B-spline order |
| $n_x$ | 48 | Knot grid resolution (half-hour steps over 24 h) |

**Estimated linear parameters**

| Symbol | Value | Description |
|--------|-------|-------------|
| $\phi_1$ | 0.9274 | Autoregressive coefficient |
| $\beta_1$ | 0.0056 | Ambient temperature gain |
| $\beta_2$ | 0.0449 | Valve heating gain |
| $\beta_3$ | 0.0487 | Solar gain coefficient |

**Estimated nonlinear parameters**

| Symbol | Value | Description |
|--------|-------|-------------|
| $\alpha_1$ | −11.28 | Sigmoid slope of the thermostatic valve |
| $\alpha_2$ | −94.03 | Sigmoid offset of the thermostatic valve |

## Output

The following figures are saved upon execution:

| File | Description |
|------|-------------|
| `FMU_Experimental_noise.png` | FMU output and experimental temperature |
| `FMU_ARX_M4_noise.png` | FMU output and M4 ARX predictions |
| `FMU_ARX_M4_Exp_noise.png` | FMU, ARX predictions, and experimental temperature |
| `Zoom_FMU_ARX_M4_noise.png` | Zoomed comparison of FMU and ARX (steps 200–350) |
| `Tset_ARX_M4_noise.png` | ARX predictions and temperature setpoint |
| `Zoom_Tset_noise.png` | Zoomed view with setpoint (steps 300–600) |




## Run

```
jupyter notebook M4_FMU_noise.ipynb
```
